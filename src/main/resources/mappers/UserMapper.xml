<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.dao.UserDao">

    <!-- Result Map -->
    <resultMap id="UserResultMap" type="User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="email" column="email" />
        <result property="fullName" column="full_name" />
        <result property="phoneNumber" column="phone_number" /> <!-- 추가: 전화번호 매핑 -->
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <!-- Select all users -->
    <select id="findAll" resultMap="UserResultMap"> SELECT id, username, email, full_name,
        phone_number, created_at, updated_at <!-- 추가: phone_number --> FROM users ORDER BY created_at DESC </select>

    <!-- Select user by ID -->
    <select id="findById" resultMap="UserResultMap" parameterType="long"> SELECT id, username,
        email, full_name, phone_number, created_at, updated_at <!-- 추가: phone_number --> FROM users WHERE id = #{id} </select>

    <!-- Select user by username -->
    <select id="findByUsername" resultMap="UserResultMap" parameterType="string"> SELECT id,
        username, email, full_name, phone_number, created_at, updated_at <!-- 추가: phone_number --> FROM users WHERE username
        = #{username} </select>

    <!-- Insert new user -->
    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id"> INSERT INTO
        users (username, email, full_name, phone_number) <!-- 추가: phone_number 컬럼 --> VALUES (#{username}, #{email},
        #{fullName}, #{phoneNumber}) <!-- 추가: phoneNumber 값 -->
    </insert>

    <!-- Update user -->
    <update id="update" parameterType="User"> UPDATE users SET username = #{username}, email =
        #{email}, full_name = #{fullName}, phone_number = #{phoneNumber} <!-- 추가: 전화번호 업데이트 --> WHERE id = #{id} </update>

    <!-- Delete user by ID -->
    <delete id="delete" parameterType="long"> DELETE FROM users WHERE id = #{id} </delete>

    <!-- Count total users -->
    <select id="count" resultType="long"> SELECT COUNT(*) FROM users </select>

</mapper>